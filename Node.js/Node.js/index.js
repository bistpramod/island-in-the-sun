(function (exports, require, module, __filename, __dirname) {
});  // its an IIFE function wrapper for every module in node.js and it is used for scoping and module management.


console.log(__filename)
console.log(__dirname)  // just removes the file name from the path and gives only the directory path.